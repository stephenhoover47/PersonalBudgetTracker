project requirements :

FIRST VERSION:

SECOND VERSION: 

LATER VERSIONS
- to account for the user case when person pays for other people on a credit card and receives venmo payments for that user getting reimbursed, the user should be able to allocate the positive transaction from revenue to a cost category bucket. example - i pay $200 for dining for a 4 person meal and 3 friends venmo me a total of $140, that means the charge to the dining category should only be $60, and I would like to associate that venmo to the dining transaction (this would have to be done manually from the user on the front end by choice)
- create new categories based on user input (front end)
- alerts to user or some type of grouping when a new statement is posted from a connected account that shows user all transactions on that statement, with total cost.
- allow user to decide which transactions should be split on each statement (default split is 50/50). this would mean the user only takes their portion of the costs, the other portion needs to go somewhere though, so maybe this is a 2-player app, or their is a feature that shows total cost and user cost (which would be their portion of the transaction costs)
- based on the above statement, a requirement may become that two users can share an account (i.e. connect accounts and when they login they see all transactions for both users connected accounts. but with the split they can see their own transaction costs and the second users transaction costs, along with a total of both)
